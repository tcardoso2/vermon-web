let cli = require('t-motion-detector').Cli;

/**
 * Add Description
 * @param {integer} Add arg
 * @param {string} static_addr, is the relative URL of the static resources, it defaults to the 
 * @example  add example
});
 */
class Commander{
  
  constructor(){
  }
}

exports.Commander = Commander;